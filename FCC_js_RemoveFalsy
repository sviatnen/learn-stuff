function bouncer(arr) {
  var arr2=arr.filter(function(item){
   return item!== false && item!=="" && item!==null && item!==0 && item!== undefined && item==item;
  });
  return arr2;
}

bouncer([7, "ate", "", false, 9]);
